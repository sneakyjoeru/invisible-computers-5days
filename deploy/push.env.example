# Copy to  config/push.env  on the Orange Pi and fill in your host + destination.
# Consumed by deploy/push_to_website.sh (never commit the real file — config/ is gitignored).
WEBSITE_HOST=user@your-host.example
WEBSITE_DEST=/var/www/html/YOUR_PATH/calendar.png
